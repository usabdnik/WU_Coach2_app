# 🚀 Быстрый старт: Автоматическая синхронизация CRM (10 минут)

## ✅ Что у вас уже готово:

- ✅ GitHub CLI установлен и авторизован
- ✅ GitHub Actions workflow создан
- ✅ Автоматический скрипт готов

---

## 📋 Что нужно сделать (2 шага):

### Шаг 1: Создайте Google Service Account (5 минут)

**Откройте инструкцию:**
```bash
open scripts/CREATE_GOOGLE_SERVICE_ACCOUNT.md
```

Или просто откройте файл `CREATE_GOOGLE_SERVICE_ACCOUNT.md` в этой папке.

**Что нужно сделать:**
1. Зайти в Google Cloud Console
2. Создать Service Account (3 клика)
3. Скачать JSON файл с ключами
4. Дать доступ к Google Sheets (Share → вставить email)

**Результат:** У вас будет JSON файл `your-project-xxxxx.json`

---

### Шаг 2: Запустите автоматический скрипт (5 минут)

```bash
cd /Users/nikitaizboldin/SuperClaude/WU_Coach2_GitHub_SpecKit/WU_Coach2_GH_SK
./scripts/setup-github-secrets.sh
```

**Что будет:**
1. Скрипт проверит что GitHub CLI авторизован ✅ (уже готово!)
2. Попросит ввести данные:
   - Supabase URL (есть значение по умолчанию)
   - Supabase Service Role Key (из Supabase Dashboard)
   - Google Sheets ID (из URL таблицы)
   - Email и Private Key из JSON файла (просто скопировать)
3. Автоматически добавит ВСЕ секреты в GitHub

**Время:** ~5 минут (просто копировать и вставлять)

---

## 🎉 Что получите:

После выполнения этих 2 шагов:

- ✅ Все секреты добавлены в GitHub
- ✅ Автоматическая синхронизация каждые 15 минут
- ✅ Новые клиенты из CRM появляются в PWA автоматически
- ✅ Можно запустить синхронизацию вручную одной кнопкой

---

## 🔧 Дополнительно (опционально):

### Изменить частоту синхронизации

Откройте `.github/workflows/crm-sync.yml`, строка 6:

```yaml
# Текущая настройка: каждые 15 минут
- cron: '*/15 * * * *'

# Каждые 5 минут:
- cron: '*/5 * * * *'

# Каждые 30 минут:
- cron: '*/30 * * * *'

# Каждый час:
- cron: '0 * * * *'
```

Закоммитьте изменение → push в GitHub.

### Запустить синхронизацию вручную

**Способ 1: Через GitHub UI**
1. Откройте: https://github.com/usabdnik/WU_Coach2_app/actions/workflows/crm-sync.yml
2. Нажмите "Run workflow" → "main" → "Run workflow"

**Способ 2: Через командную строку**
```bash
gh workflow run crm-sync.yml --repo usabdnik/WU_Coach2_app
```

---

## 📞 Помощь

Если что-то не работает:
1. Проверьте логи workflow в GitHub Actions
2. Убедитесь что все 5 секретов добавлены
3. Проверьте что Service Account имеет доступ к Google Sheets

---

**Общее время на настройку: ~10 минут**
**Результат: Автоматическая синхронизация 24/7** 🚀
