# 🧪 Тест подключения к Supabase

**Дата**: 2025-11-10
**Статус**: Готов к тестированию ⏳

---

## Подготовка

У вас есть **два варианта** тестирования:

### Вариант 1: Локальный Supabase (рекомендуется для разработки)

**Требования**:
- ✅ Docker Desktop установлен и запущен
- ✅ Supabase CLI установлен

**Шаги запуска**:

```bash
# 1. Проверьте, что Docker работает
docker ps

# 2. Запустите Supabase локально
supabase start

# Вы увидите:
# Started supabase local development setup.
#
#          API URL: http://127.0.0.1:54321
#      GraphQL URL: http://127.0.0.1:54321/graphql/v1
#           DB URL: postgresql://postgres:postgres@127.0.0.1:54322/postgres
#       Studio URL: http://127.0.0.1:54323
#     Inbucket URL: http://127.0.0.1:54324
#       JWT secret: super-secret-jwt-token-with-at-least-32-characters-long
#         anon key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
# service_role key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**Что использовать в тесте**:
- **URL**: `http://127.0.0.1:54321`
- **Anon Key**: (показан в выводе команды `supabase start`)

---

### Вариант 2: Облачный Supabase

**Требования**:
- ✅ Аккаунт на [supabase.com](https://supabase.com)
- ✅ Созданный проект

**Где взять credentials**:

1. Откройте [supabase.com/dashboard](https://supabase.com/dashboard)
2. Выберите свой проект
3. Перейдите в **Settings** → **API**
4. Скопируйте:
   - **Project URL** (например, `https://xxxxx.supabase.co`)
   - **anon public** key

---

## Запуск теста

### Шаг 1: Запустите локальный веб-сервер

**ВАЖНО**: Файл нужно открыть через HTTP-сервер, не напрямую!

```bash
# Убедитесь, что находитесь в корне проекта
cd /Users/nikitaizboldin/SuperClaude/WU_Coach2_GitHub_SpecKit/WU_Coach2_GH_SK

# Запустите сервер
python3 -m http.server 8000
```

### Шаг 2: Откройте тест в браузере

```
http://localhost:8000/test-supabase-connection.html
```

### Шаг 3: Выберите тип подключения

- **Локальный (Docker)** - если запустили `supabase start`
- **Облачный проект** - если используете supabase.com

### Шаг 4: Введите credentials

**Для локального**:
- URL уже заполнен: `http://127.0.0.1:54321`
- Anon Key уже заполнен (стандартный локальный ключ)

**Для облачного**:
- Вставьте свой Project URL
- Вставьте свой anon public key

### Шаг 5: Нажмите "Проверить подключение"

---

## Ожидаемые результаты

### ✅ Успешное подключение

Вы должны увидеть:

```
✅ Supabase клиент создан
✅ REST API доступен
✅ Доступ к базе данных работает (таблица не найдена - это нормально)
ℹ️ Анонимный доступ (пользователь не авторизован - это нормально для теста)
🎉 Подключение к Supabase работает!
```

### ❌ Возможные ошибки

#### Ошибка: "Failed to fetch"

**Причина (Локальный)**:
- Docker не запущен
- Supabase не запущен (`supabase start` не выполнен)

**Решение**:
```bash
# Проверьте Docker
docker ps

# Запустите Supabase
supabase start
```

**Причина (Облачный)**:
- Неправильный URL
- Неправильный ключ

**Решение**:
- Проверьте URL в Settings → API
- Скопируйте правильный anon public key

#### Ошибка: CORS

**Причина**: Файл открыт напрямую через `file://`

**Решение**:
```bash
# Обязательно используйте HTTP-сервер
python3 -m http.server 8000

# Затем откройте
http://localhost:8000/test-supabase-connection.html
```

#### Ошибка: "REST API недоступен (401)"

**Причина**: Неправильный ключ API

**Решение**:
- Проверьте, что используете именно **anon public** key, не service_role
- Скопируйте ключ заново из dashboard

---

## Что проверяет тест?

1. **Создание клиента Supabase** - проверяет, что SDK загружен
2. **REST API доступность** - проверяет HTTP-соединение
3. **Доступ к БД** - проверяет, что можно выполнять запросы
4. **Аутентификация** - проверяет, что anon key работает

---

## Следующие шаги после успешного теста

1. ✅ **Создайте схему БД**
   - Локально: Создайте `supabase/migrations/20251110_initial_schema.sql`
   - Облачно: Используйте SQL Editor в Supabase Studio

2. ✅ **Настройте RLS policies**
   - Определите правила доступа к данным
   - Для MVP: разрешите анонимный доступ ко всем таблицам

3. ✅ **Интегрируйте в приложение**
   - Добавьте Supabase SDK в `index.html`
   - Замените Google Sheets API на Supabase queries

---

## Проверка локального Supabase

Если используете локальный Supabase, откройте **Supabase Studio**:

```
http://127.0.0.1:54323
```

Здесь вы можете:
- 📊 Просматривать таблицы
- 🔍 Выполнять SQL-запросы
- 👥 Управлять пользователями
- 🔐 Настраивать RLS policies

---

## Остановка локального Supabase

Когда закончите:

```bash
supabase stop
```

---

## Чеклист тестирования

- [ ] Docker запущен (для локального варианта)
- [ ] Supabase запущен (`supabase start`)
- [ ] Веб-сервер запущен (`python3 -m http.server 8000`)
- [ ] Тест открыт через `http://localhost:8000/test-supabase-connection.html`
- [ ] Credentials введены правильно
- [ ] Тест прошел успешно (🎉 Подключение работает!)
- [ ] Консоль браузера без ошибок (F12 → Console)

---

## 📞 Если что-то не работает

**Откройте консоль браузера** (F12 → Console) и пришлите:

1. Скриншот результатов теста
2. Текст ошибок из консоли
3. Какой вариант используете (локальный/облачный)

Я помогу разобраться! 🚀

---

## Следующая задача

После успешного теста переходим к:
1. ✅ Создание схемы БД (`supabase/migrations/`)
2. ✅ Миграция данных из Google Sheets
3. ✅ Интеграция Supabase в PWA
