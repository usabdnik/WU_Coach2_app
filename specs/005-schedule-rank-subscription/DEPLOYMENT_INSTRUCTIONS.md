# Инструкции по деплою миграции и продолжению работы

**Фича:** 005-schedule-rank-subscription
**Дата:** 2025-11-11
**Прогресс:** 30% (Фазы 1-3 из 8 завершены)

---

## ✅ Что уже сделано

### Phase 1: Database Migration (готова к деплою)
- ✅ Создан файл миграции `supabase/migrations/20251111000002_add_schedule_rank_fields.sql`
- ✅ Добавлены колонки: `schedule`, `rank_start`, `rank_end`, `rank_history`
- ⏳ **ОЖИДАЕТ ДЕПЛОЯ** - проблемы с сетевым подключением к Supabase

### Phase 2: Foundational Infrastructure (завершена)
- ✅ CSS стили для расписания, разрядов, фильтров (строки 634-817 в index.html)
- ✅ Функция `transformSupabaseAthlete()` расширена новыми полями (строка 2018-2021)
- ✅ localStorage автоматически сохраняет новые данные

### Phase 3: Schedule Display (завершена)
- ✅ Функция `formatScheduleDisplay()` создана (строки 1720-1734)
- ✅ Отображение расписания в профиле атлета (строки 1749-1754)
- ✅ Поддержка 3 форматов: фиксированное расписание, самозапись, пусто

---

## 🚨 КРИТИЧЕСКИ: Задеплоить миграцию

### Проблема
`supabase db push` падает с ошибкой сети:
```
failed to connect to postgres: tls error (read tcp ... connection reset by peer)
```

### Решение: Ручной деплой через Supabase Dashboard

#### Шаг 1: Откройте Supabase Dashboard
```
https://supabase.com/dashboard/project/mjkssesvhowmncyctmvs
```

#### Шаг 2: Перейдите в SQL Editor
**Database → SQL Editor → New query**

#### Шаг 3: Скопируйте и выполните миграцию
```sql
-- Migration: Add schedule, rank_start, rank_end, rank_history columns to athletes table
-- Feature: 005-schedule-rank-subscription
-- Date: 2025-11-11

-- Add new columns to athletes table
ALTER TABLE public.athletes
    ADD COLUMN IF NOT EXISTS schedule TEXT NULL,
    ADD COLUMN IF NOT EXISTS rank_start TEXT NULL,
    ADD COLUMN IF NOT EXISTS rank_end TEXT NULL,
    ADD COLUMN IF NOT EXISTS rank_history JSONB NULL DEFAULT '[]'::jsonb;

-- Add column comments for documentation
COMMENT ON COLUMN public.athletes.schedule IS
    'Athlete attendance schedule. Format: "День ЧЧ:ММ, День ЧЧ:ММ" (e.g., "Пн 18:00, Ср 19:00") for fixed schedule, or literal "Самозапись" for self-registration. NULL if not set.';

COMMENT ON COLUMN public.athletes.rank_start IS
    'Athletic rank at season start. Values: "III юношеский разряд", "II юношеский разряд", "I юношеский разряд", "III взрослый разряд", "II взрослый разряд", "I взрослый разряд", "КМС", "МС", "МСМК", "Без разряда". NULL if not set.';

COMMENT ON COLUMN public.athletes.rank_end IS
    'Athletic rank achieved by season end. Same values as rank_start. NULL if season not complete or not yet recorded.';

COMMENT ON COLUMN public.athletes.rank_history IS
    'Historical rank progression data across multiple seasons. Format: [{"season": "2024-2025", "rank_start": "I юношеский разряд", "rank_end": "III взрослый разряд"}]. Empty array [] if no history.';
```

#### Шаг 4: Нажмите "Run" и проверьте результат
Должно быть: `Success. No rows returned`

#### Шаг 5: Проверьте колонки в таблице
**Database → Tables → athletes → Columns**

Должны появиться новые колонки:
- `schedule` (text, nullable)
- `rank_start` (text, nullable)
- `rank_end` (text, nullable)
- `rank_history` (jsonb, nullable, default: `[]`)

---

## 📋 Тестирование Phase 1-3

### 1. Откройте PWA в браузере
```bash
# Если есть локальный сервер:
python3 -m http.server 8000
# Откройте: http://localhost:8000/index.html

# Или просто откройте файл:
open index.html  # macOS
```

### 2. Откройте консоль браузера (F12)
Проверьте, что нет ошибок при загрузке.

### 3. Тест transformSupabaseAthlete (Phase 2)
В консоли браузера:
```javascript
// Проверьте, что новые поля включены в объект атлета
const athlete = athletesData[0];
console.log({
  schedule: athlete.schedule,
  rank_start: athlete.rank_start,
  rank_end: athlete.rank_end,
  rank_history: athlete.rank_history
});
```

**Ожидаемый результат:**
```javascript
{
  schedule: "",  // или null, или строка с расписанием
  rank_start: null,
  rank_end: null,
  rank_history: []
}
```

### 4. Тест formatScheduleDisplay (Phase 3)
В консоли браузера:
```javascript
// Тест пустого расписания
formatScheduleDisplay('');  // → "(Не указано)"

// Тест самозаписи
formatScheduleDisplay('Самозапись');  // → badge "📝 Самозапись"

// Тест фиксированного расписания
formatScheduleDisplay('Пн 18:00, Ср 19:00');  // → 2 badges "🕐 Пн 18:00" "🕐 Ср 19:00"
```

### 5. Тест отображения в профиле атлета
1. Откройте любого атлета (кликните на карточку)
2. Проверьте секцию "Расписание" в модальном окне
3. Должно отображаться: `(Не указано)` (пока нет данных)

**✅ Если всё работает → переходите к продолжению реализации**

---

## 🔄 Продолжение работы: Phase 4-8

### Осталось реализовать (66 задач)

**📋 Phase 4: Schedule Editing** (18 задач, P1 MVP)
- T019-T036: Создание модального окна редактирования расписания
- Форма с переключением: фиксированное расписание ↔ самозапись
- Сохранение в Supabase + localStorage

**📋 Phase 5: Subscription Filtering** (17 задач, P2)
- T037-T053: Фильтр "Абонементы за сезон"
- Интеграция с Moyklass API
- Кэширование в localStorage (24 часа)

**📋 Phase 6: Season Start Rank** (12 задач, P3)
- T054-T065: Dropdown выбор разряда начала сезона
- 9 разрядов + "Без разряда"
- Сохранение и отображение

**📋 Phase 7: Season End Rank** (11 задач, P3)
- T066-T076: Dropdown выбор разряда конца сезона
- Отображение прогрессии (стрелка start → end)

**📋 Phase 7.5: Historical Rank Data** (9 задач)
- T076a-T076i: История разрядов по сезонам
- JSONB `rank_history` поле
- Модальное окно просмотра истории

**📋 Phase 8: Polish & Validation** (19 задач)
- T077-T095: Логирование, обработка ошибок, валидация
- Тестирование офлайн/онлайн режимов
- Финальная проверка quickstart.md

### Запуск продолжения в новой сессии

```bash
# 1. Убедитесь, что миграция задеплоена (см. выше)

# 2. Проверьте текущую ветку
git status
git branch  # Должна быть: 005-schedule-rank-subscription

# 3. В Claude Code скажите:
"Продолжи реализацию фичи 005-schedule-rank-subscription.
Фазы 1-3 завершены (30%).
Начинаем с Phase 4: Schedule Editing (T019-T036).
Файл tasks.md содержит полный список задач."

# 4. Claude загрузит контекст из tasks.md и продолжит работу
```

---

## 📁 Ключевые файлы

| Файл | Описание |
|------|----------|
| `specs/005-schedule-rank-subscription/tasks.md` | **Главный файл задач** (95 tasks, отмечено 29) |
| `specs/005-schedule-rank-subscription/spec.md` | Спецификация (User Stories, Requirements) |
| `specs/005-schedule-rank-subscription/plan.md` | Технический план (Tech Stack, Architecture) |
| `specs/005-schedule-rank-subscription/data-model.md` | Схема БД (schedule, rank fields) |
| `specs/005-schedule-rank-subscription/quickstart.md` | Quickstart guide с примерами кода |
| `supabase/migrations/20251111000002_add_schedule_rank_fields.sql` | **Миграция (требует деплоя!)** |
| `index.html` | Главный файл PWA (строки 634-817: CSS, 1720-1754: JS) |

---

## ⚠️ Важные заметки

### Проблема с Supabase connectivity
- **Причина:** Network/Docker issues, connection reset by peer
- **Решение:** Ручной деплой через Dashboard (см. выше)
- **Альтернатива:** Попробовать `supabase db push` позже, когда сеть восстановится

### PostgreSQL@15 установлен
```bash
brew services start postgresql@15  # Запустить локально (опционально)
```

### Moyklass API интеграция
- API key уже настроен (см. `migration/import-from-moyklass.js`)
- Для Phase 5 (Subscription Filter) потребуется:
  - Проверить `MOYKLASS_API_KEY` в env
  - Тестовый запрос к `/v1/subscriptions`

### Структура index.html
- **CSS:** Строки 11-817 (добавлено 184 строки стилей)
- **HTML:** Строки 820-1500 (требуется добавить modals для Phase 4)
- **JS:** Строки 1500-2800+ (добавлено ~30 строк, требуется +200 для Phase 4-8)

---

## 🎯 Чеклист перед продолжением

- [ ] Миграция задеплоена через Supabase Dashboard
- [ ] Колонки видны в таблице `athletes`
- [ ] PWA загружается без ошибок в консоли
- [ ] `transformSupabaseAthlete()` включает новые поля
- [ ] `formatScheduleDisplay()` работает корректно
- [ ] Секция "Расписание" отображается в профиле атлета
- [ ] Git коммит создан и сохранён локально
- [ ] tasks.md актуален (29/95 задач отмечено)

---

**Готовы продолжать?** 🚀

После выполнения чеклиста, начинайте новую сессию Claude Code с запросом:

```
Продолжи реализацию feature 005-schedule-rank-subscription.
Фазы 1-3 завершены. Начни с Phase 4: Schedule Editing.
Используй tasks.md как руководство.
```

Claude загрузит контекст и продолжит с задачи T019.
