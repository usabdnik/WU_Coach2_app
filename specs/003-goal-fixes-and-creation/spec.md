# Feature Specification: Goal Fixes & Creation

**Feature Branch**: `003-goal-fixes-and-creation`
**Created**: 2025-11-03
**Status**: Draft
**Input**: User description: "проблема таже: иванов петр. выход силой и передний вис нельзя редактировать нет отклика на нажитие на карандаш. две цели отжимание на одной руке можно реадктировать, у остальных цели не редактируються. Так же не понятно когда идет процесс синхронизации а когда нет. так же нужно добавить возможность добавления целей"

## User Scenarios & Testing *(mandatory)*

### User Story 1 - Исправление редактирования всех целей (Priority: P1) 🎯 MVP

Тренер не может редактировать некоторые цели ("Выход силой", "передний вис" у Иванова Петра). При клике на карточку цели с карандашом не происходит открытие модала редактирования. Другие цели (например, "отжимание на одной руке") редактируются корректно. Тренер должен иметь возможность редактировать ЛЮБУЮ цель независимо от типа упражнения.

**Why this priority**: Критический багфикс - блокирует базовую функциональность редактирования целей для части данных. Без исправления Feature 001 работает только частично. Это P1 потому что ломает основной функционал MVP (редактирование целей).

**Independent Test**: Можно полностью протестировать открыв Иванова Петра → кликнув на цель "Выход силой" → убедившись что модал редактирования открывается → изменив даты → сохранив и проверив что изменения применились.

**Acceptance Scenarios**:

1. **Given** тренер открыл карточку Иванова Петра с целями "Выход силой" и "передний вис", **When** тренер кликает на карточку цели "Выход силой", **Then** модал редактирования целей открывается с предзаполненными датами этой цели

2. **Given** модал редактирования открыт для цели "Выход силой", **When** тренер изменяет дату начала на 2025-11-10 и дату окончания на 2025-12-10 и нажимает "Сохранить", **Then** изменения сохраняются в localStorage, добавляется запись в pendingChanges, модал закрывается, карточка цели отображает обновлённые даты

3. **Given** тренер открыл любую карточку спортсмена с целями, **When** тренер кликает на ЛЮБУЮ цель независимо от типа упражнения, **Then** модал редактирования открывается корректно для всех типов целей без исключений

4. **Given** тренер редактировал цель с багфиксом, **When** тренер перезагружает страницу, **Then** изменённые даты цели сохранены в localStorage и отображаются корректно

5. **Given** существуют цели без ID в localStorage, **When** приложение загружается, **Then** миграция автоматически генерирует UUID для всех целей без ID

6. **Given** цель была отредактирована офлайн, **When** тренер подключается к интернету и синхронизирует, **Then** изменения корректно отправляются на сервер и подтверждаются

---

### User Story 2 - Визуальная индикация процесса синхронизации (Priority: P2)

Тренер не понимает когда идёт процесс синхронизации с Google Sheets, а когда нет. Кнопка "Синхронизировать" не даёт визуальной обратной связи о текущем состоянии операции. Тренер должен чётко видеть 3 состояния: покой (можно запустить), в процессе (идёт синхронизация), завершено (успех/ошибка).

**Why this priority**: UX улучшение - помогает тренеру понимать состояние приложения, но не блокирует функционал. Синхронизация уже работает, просто непонятно её состояние. P2 потому что улучшает пользовательский опыт, но не критично для работоспособности.

**Independent Test**: Можно полностью протестировать нажав кнопку "Синхронизировать" → наблюдая визуальную индикацию (спиннер, прогресс-бар или анимацию) → видя финальное состояние (успех с галочкой или ошибка с описанием).

**Acceptance Scenarios**:

1. **Given** тренер находится в приложении с несинхронизированными изменениями, **When** тренер смотрит на кнопку синхронизации, **Then** кнопка показывает оранжевый фон и текст "Синхронизировать (N ред.)" где N = количество несинхронизированных изменений

2. **Given** тренер нажал кнопку "Синхронизировать", **When** начинается процесс синхронизации, **Then** кнопка становится disabled, текст меняется на "Синхронизация..." с анимацией спиннера/индикатора прогресса

3. **Given** идёт процесс синхронизации, **When** синхронизация успешно завершается, **Then** кнопка на 2 секунды показывает зелёный фон с текстом "✅ Синхронизировано" затем возвращается в обычное состояние "⚡ Синхронизировать"

4. **Given** идёт процесс синхронизации, **When** происходит ошибка синхронизации (нет интернета или ошибка сервера), **Then** кнопка на 3 секунды показывает красный фон с текстом "❌ Ошибка синхронизации" затем возвращается в исходное состояние с возможностью повторить

5. **Given** идёт процесс синхронизации большого количества изменений (>10), **When** синхронизируется каждое изменение, **Then** текст кнопки обновляется показывая прогресс "Синхронизация... (3/15)" с индикацией обработанных/всего изменений

6. **Given** синхронизация в процессе, **When** тренер пытается нажать кнопку синхронизации повторно, **Then** кнопка disabled и клик игнорируется (нельзя запустить параллельную синхронизацию)

---

### User Story 3 - Добавление новых целей (Priority: P2)

Тренер может только редактировать существующие цели, но не может создавать новые. Тренер должен иметь возможность добавить новую цель для спортсмена с указанием типа упражнения, дат начала/окончания и описания прямо из карточки спортсмена.

**Why this priority**: Новая функциональность расширяющая возможности работы с целями. P2 потому что редактирование уже работает (после P1 багфикса), а добавление целей - логичное дополнение но не критичное для MVP.

**Independent Test**: Можно полностью протестировать открыв карточку любого спортсмена → нажав кнопку "Добавить цель" → заполнив форму (тип упражнения, даты, описание) → сохранив и проверив что новая цель появилась в списке целей спортсмена.

**Acceptance Scenarios**:

1. **Given** тренер открыл карточку спортсмена, **When** тренер нажимает кнопку "➕ Добавить цель" в секции целей, **Then** открывается модал создания новой цели с пустой формой

2. **Given** модал создания цели открыт, **When** тренер заполняет форму: тип упражнения (dropdown "Подтягивания" / "Отжимания от пола" / "Отжимания от брусьев" / "Выход силой" / "Передний вис"), дата начала (date picker), дата окончания (date picker), описание (textarea опционально), **Then** все поля доступны для ввода с валидацией

3. **Given** тренер заполнил форму создания цели, **When** тренер вводит дату окончания раньше даты начала, **Then** появляется сообщение об ошибке "Дата окончания должна быть позже даты начала" красным текстом под полями дат

4. **Given** форма создания цели заполнена корректно, **When** тренер нажимает "Создать цель", **Then** новая цель создаётся с уникальным UUID, сохраняется в localStorage, добавляется в pendingChanges как type:'goal' action:'create', модал закрывается, новая цель отображается в списке целей спортсмена

5. **Given** новая цель создана офлайн, **When** тренер синхронизируется с Google Sheets, **Then** новая цель отправляется на сервер и добавляется в таблицу Goals с присвоением серверного ID

6. **Given** тренер создаёт цель с описанием более 200 символов, **When** тренер пытается сохранить, **Then** появляется ошибка валидации "Описание не может превышать 200 символов"

7. **Given** тренер открыл модал создания цели, **When** тренер нажимает "Отмена" или иконку закрытия (×), **Then** модал закрывается без сохранения, введённые данные не сохраняются

---

### Edge Cases

#### Редактирование целей (User Story 1)

- **Что происходит если цель редактируется офлайн, а до синхронизации цель удаляется другим пользователем на сервере?**
  При синхронизации сервер вернёт ошибку "Цель не найдена". Приложение должно показать тренеру уведомление "Цель была удалена другим пользователем. Ваши изменения не применены." и удалить цель из локальных данных.

- **Что происходит если одна цель редактируется несколько раз офлайн подряд?**
  Queue coalescing (уже реализовано) объединит все изменения одной цели в одно финальное изменение перед синхронизацией. Только последнее состояние будет отправлено на сервер.

- **Что происходит если у цели отсутствует поле exerciseName при загрузке?**
  Миграция должна обработать этот случай установив exerciseName в "Без названия" и логировав предупреждение в консоль. Цель останется редактируемой.

- **Что происходит если миграция UUID запускается повторно для целей у которых уже есть ID?**
  Функция migrateGoalIds() проверяет `if (!goal.id)` перед генерацией UUID. Если ID уже существует, цель пропускается. Повторный вызов безопасен и не создаёт дубликатов.

#### Индикация синхронизации (User Story 2)

- **Что происходит если синхронизация прерывается из-за потери интернета в середине процесса?**
  Fetch запрос выбросит network error. Приложение должно показать "❌ Ошибка синхронизации: Проверьте подключение к интернету" на 3 секунды, затем вернуть кнопку в состояние готовности для повторной попытки. Несинхронизированные изменения остаются в pendingChanges.

- **Что происходит если сервер возвращает 5xx ошибку во время синхронизации?**
  Приложение должно показать "❌ Ошибка синхронизации: Проблема на сервере. Попробуйте позже." на 3 секунды. Изменения остаются в pendingChanges для повторной попытки. После 3 неудачных попыток подряд показать более детальное сообщение с предложением связаться с поддержкой.

- **Что происходит если синхронизация занимает более 30 секунд?**
  Кнопка остаётся в состоянии "Синхронизация..." с анимацией, показывая прогресс если доступно. После 30 секунд добавить текст "(это занимает больше времени, чем обычно)". После 60 секунд показать предупреждение "Синхронизация идёт дольше обычного. Проверьте интернет или попробуйте позже."

- **Что происходит если пользователь закрывает вкладку/приложение во время синхронизации?**
  Браузер может прервать fetch запросы. При следующем открытии приложения несинхронизированные изменения останутся в pendingChanges (сохранены в localStorage), индикатор покажет оранжевый фон с количеством ожидающих изменений.

#### Добавление целей (User Story 3)

- **Что происходит если тренер создаёт цель для упражнения которого нет в справочнике exercisesData?**
  Dropdown должен показывать только упражнения из exercisesData. Если справочник пуст, показать сообщение "Сначала синхронизируйтесь с Google Sheets для загрузки упражнений" и disable кнопку "Добавить цель".

- **Что происходит если тренер создаёт несколько целей для одного спортсмена с одинаковыми датами и типом упражнения?**
  Приложение не должно блокировать создание дубликатов на клиенте (валидация данных - ответственность бизнес-логики на сервере). Обе цели создаются с разными UUID и отображаются отдельно. Если это нежелательно, тренер может удалить лишнюю цель.

- **Что происходит если тренер создаёт цель с датой начала в прошлом?**
  Валидация не должна блокировать даты в прошлом (тренер может создавать цели задним числом для архивных данных). Предупреждение: "Дата начала в прошлом - цель будет создана как есть" но позволить сохранить.

- **Что происходит если форма создания цели открыта, а тренер переключается на другую карточку спортсмена?**
  При открытии новой карточки спортсмена модал создания цели должен закрыться автоматически без сохранения. При открытии модал всегда привязан к текущему видимому спортсмену.

## Requirements *(mandatory)*

### Functional Requirements

#### User Story 1: Исправление редактирования целей

- **FR-001**: Система ДОЛЖНА генерировать UUID для всех целей без ID при загрузке из Google Sheets (реализовано в bugfix commit ff46171, проверить работоспособность)
- **FR-002**: Система ДОЛЖНА запускать миграцию migrateGoalIds() при загрузке приложения для всех целей в localStorage без ID (реализовано в bugfix commit ff46171, проверить работоспособность)
- **FR-003**: Система ДОЛЖНА логировать в консоль количество целей для которых был сгенерирован ID при миграции для отладки
- **FR-004**: Карточки всех целей ДОЛЖНЫ быть кликабельными с cursor: pointer независимо от типа упражнения
- **FR-005**: Клик на любую карточку цели ДОЛЖЕН вызывать openGoalEditModal(goal.id) с передачей корректного UUID цели
- **FR-006**: Функция openGoalEditModal() ДОЛЖНА валидировать существование goal.id и логировать ошибку если ID undefined
- **FR-007**: Модал редактирования ДОЛЖЕН корректно предзаполняться датами для целей любого типа упражнения
- **FR-008**: Сохранение отредактированной цели ДОЛЖНО добавлять запись в pendingChanges с type: 'goal_edit' и goalId
- **FR-009**: Счётчик pendingChanges в кнопке синхронизации ДОЛЖЕН корректно отображать количество goal_edit изменений (реализовано в bugfix commit ff46171, проверить работоспособность)
- **FR-010**: Тестирование ДОЛЖНО включать проверку на реальных данных Иванова Петра с целями "Выход силой" и "передний вис"

#### User Story 2: Визуальная индикация синхронизации

- **FR-011**: Кнопка синхронизации ДОЛЖНА иметь 4 визуальных состояния: покой (серый фон), ожидает синхронизации (оранжевый фон), в процессе (disabled + спиннер), завершено (зелёный/красный 2-3 сек)
- **FR-012**: В состоянии "ожидает синхронизации" кнопка ДОЛЖНА показывать текст "🔄 Синхронизировать (N ред.)" где N = pendingChanges.length
- **FR-013**: При клике на кнопку синхронизации кнопка ДОЛЖНА стать disabled и показать текст "⏳ Синхронизация..." с CSS анимацией вращения спиннера
- **FR-014**: Для операций синхронизации >10 изменений кнопка ДОЛЖНА показывать прогресс "Синхронизация... (3/15)" обновляемый при обработке каждого изменения
- **FR-015**: После успешной синхронизации кнопка ДОЛЖНА показать "✅ Синхронизировано" с зелёным фоном на 2 секунды затем вернуться в состояние покоя
- **FR-016**: При ошибке синхронизации кнопка ДОЛЖНА показать "❌ Ошибка синхронизации" с красным фоном на 3 секунды затем вернуться в предыдущее состояние с несинхронизированными изменениями
- **FR-017**: Текст ошибки синхронизации ДОЛЖЕН быть информативным: "Нет интернета", "Ошибка сервера", "Проблема с данными" в зависимости от типа ошибки
- **FR-018**: Система ДОЛЖНА показывать предупреждение если синхронизация длится >30 секунд: "(это занимает больше времени, чем обычно)"
- **FR-019**: Система ДОЛЖНА блокировать повторный запуск синхронизации пока идёт текущая операция (кнопка disabled)
- **FR-020**: После прерывания синхронизации (потеря интернета) несинхронизированные изменения ДОЛЖНЫ сохраниться в pendingChanges для повторной попытки

#### User Story 3: Добавление целей

- **FR-021**: В карточке спортсмена в секции целей ДОЛЖНА быть кнопка "➕ Добавить цель" после списка существующих целей
- **FR-022**: Клик на "Добавить цель" ДОЛЖЕН открывать модал создания цели с пустой формой
- **FR-023**: Форма создания цели ДОЛЖНА содержать поля: тип упражнения (dropdown), дата начала (date input), дата окончания (date input), описание (textarea, опционально)
- **FR-024**: Dropdown типа упражнения ДОЛЖЕН быть заполнен из массива exercisesData с названиями упражнений
- **FR-025**: Поля дат начала и окончания ДОЛЖНЫ использовать native HTML5 `<input type="date">` с тёмной темой (уже реализовано в Feature 001)
- **FR-026**: Поле описания ДОЛЖНО иметь placeholder "Например: 12 подтягиваний к концу месяца" и maxlength="200"
- **FR-027**: Система ДОЛЖНА валидировать: дата окончания >= дата начала, длительность <= 1 год, описание <= 200 символов
- **FR-028**: Кнопка "Создать цель" ДОЛЖНА быть disabled если обязательные поля (тип упражнения, даты) не заполнены или валидация не пройдена
- **FR-029**: При успешном создании цели система ДОЛЖНА генерировать UUID, сохранить в goalsData, добавить в localStorage, добавить в pendingChanges как type:'goal' action:'create'
- **FR-030**: Новая цель ДОЛЖНА немедленно отображаться в списке целей спортсмена после создания
- **FR-031**: Модал создания цели ДОЛЖЕН закрываться после успешного сохранения или при клике на "Отмена" / иконку закрытия
- **FR-032**: При синхронизации новых целей сервер ДОЛЖЕН вернуть serverGeneratedId который заменяет clientGeneratedId в localStorage

### Key Entities

#### Goal (Enhanced)

Цель спортсмена для конкретного упражнения с отслеживанием прогресса.

**Attributes**:
- `id`: UUID v4 (clientGenerated или serverGenerated), уникальный идентификатор цели
- `studentId`: UUID ссылка на Athlete, к которому относится цель
- `studentName`: String, имя спортсмена (денормализация для удобства)
- `exerciseId`: UUID ссылка на Exercise
- `exerciseName`: String, название упражнения ("Подтягивания", "Выход силой" и т.д.)
- `startDate` (или `setDate`): ISO date, дата начала цели (поддержка обоих полей для совместимости)
- `endDate` (или `completionDate`): ISO date, дата окончания/выполнения цели
- `description`: String (опционально, max 200 символов), текстовое описание цели
- `targetValue`: Number (опционально), целевое значение повторений
- `notes`: String (опционально), дополнительные заметки
- `status`: Enum ['active', 'completed', 'cancelled'], статус цели (по умолчанию 'active')
- `createdAt`: ISO timestamp, дата создания записи
- `updatedAt`: ISO timestamp, дата последнего изменения (обновляется при редактировании)

**States**:
- **Active**: цель в процессе, endDate/completionDate пусто или в будущем
- **Completed**: цель выполнена, endDate/completionDate заполнено и в прошлом или status='completed'
- **Pending Sync**: цель создана/отредактирована офлайн, запись в pendingChanges существует

**Relations**:
- Belongs to one Athlete (many-to-one)
- References one Exercise type (many-to-one)

---

#### PendingChange (Enhanced for Goal Creation)

Запись в очереди несинхронизированных изменений для офлайн-first архитектуры.

**Attributes for Goal Creation**:
- `type`: String 'goal' (для создания/удаления) или 'goal_edit' (для редактирования)
- `action`: String ['create', 'delete'] (только для type='goal', не используется для goal_edit)
- `goalId`: UUID, идентификатор изменяемой/создаваемой/удаляемой цели
- `goalData`: Object (для action='create'), полные данные новой цели для отправки на сервер
- `changes`: Object (для type='goal_edit'), только изменённые поля цели
- `timestamp`: ISO timestamp, время создания записи изменения

**Example for Goal Creation**:
```json
{
  "type": "goal",
  "action": "create",
  "goalId": "client-generated-uuid",
  "goalData": {
    "studentId": "athlete-uuid",
    "studentName": "Иванов Пётр",
    "exerciseId": "exercise-uuid",
    "exerciseName": "Подтягивания",
    "startDate": "2025-11-10",
    "endDate": "2025-12-10",
    "description": "12 подтягиваний к концу месяца",
    "status": "active",
    "createdAt": "2025-11-03T12:00:00Z"
  },
  "timestamp": "2025-11-03T12:00:00Z"
}
```

## Success Criteria *(mandatory)*

### Measurable Outcomes

#### User Story 1: Исправление редактирования целей

- **SC-001**: 100% целей любого типа упражнения открывают модал редактирования при клике на карточку (протестировано на всех типах: подтягивания, отжимания, брусья, выход силой, передний вис)
- **SC-002**: Модал редактирования открывается за <300ms после клика на карточку цели на мобильных устройствах
- **SC-003**: Миграция целей без ID завершается автоматически за <1 секунду при загрузке приложения
- **SC-004**: Изменения целей сохраняются локально и отображаются корректно после перезагрузки страницы в 100% случаев
- **SC-005**: Тренер может успешно отредактировать цели "Выход силой" и "передний вис" у Иванова Петра (специфический тестовый сценарий из user feedback)

#### User Story 2: Визуальная индикация синхронизации

- **SC-006**: Тренер видит текущее состояние синхронизации в 100% случаев без необходимости проверять консоль или логи
- **SC-007**: Индикатор прогресса синхронизации обновляется в реальном времени с задержкой <500ms для каждого синхронизированного изменения
- **SC-008**: После синхронизации тренер получает визуальное подтверждение (зелёный ✅ или красный ❌) в течение 2-3 секунд
- **SC-009**: Сообщения об ошибках синхронизации понятны тренеру без технических терминов и дают чёткие инструкции (например: "Проверьте подключение к интернету" вместо "Network error")
- **SC-010**: Повторный запуск синхронизации заблокирован (кнопка disabled) пока идёт текущая операция (предотвращение параллельных запросов)

#### User Story 3: Добавление целей

- **SC-011**: Тренер может создать новую цель для спортсмена за <2 минут (время заполнения формы и сохранения)
- **SC-012**: Новая цель отображается в списке целей спортсмена немедленно после создания (<500ms задержка)
- **SC-013**: Валидация формы создания цели предотвращает 100% некорректных дат (окончание < начала, длительность >1 года)
- **SC-014**: Цели созданные офлайн успешно синхронизируются с Google Sheets при восстановлении интернета в 100% случаев
- **SC-015**: После создания 50 новых целей офлайн, все 50 корректно синхронизируются без потери данных и дубликатов
- **SC-016**: Dropdown типа упражнения загружается из актуальных данных exercisesData (отражает изменения после синхронизации с Google Sheets)
