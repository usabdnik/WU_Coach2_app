# Feature 006: Динамическая система групп

**Дата создания**: 2025-11-15
**Статус**: ✅ ЗАВЕРШЕНО
**Коммит**: `d409f90`
**Branch**: `main`

---

## Описание проблемы

Группы учеников (Начинающие, Средняя, Продвинутая, Элитная) были захардкожены в HTML и не соответствовали реальным классам школы. Требовалось заменить на реальные названия групп с возможностью легкого изменения в будущем.

---

## Требования

### Функциональные

**FR-1**: Заменить старые группы на новые
- **Было**: Начинающие, Средняя, Продвинутая, Элитная
- **Стало**: М-19, М-117, М-118, А-29, А-218, А-219

**FR-2**: Гибкость изменения групп
- Состав групп может меняться
- Должна быть возможность легко добавлять/удалять/переименовывать группы
- Изменения в одном месте → автоматическое обновление всех UI элементов

**FR-3**: Сохранение существующего функционала
- Фильтрация по группам
- Кнопка "Все группы"
- Dropdown в форме редактирования ученика
- Синхронизация с Supabase

### Нефункциональные

**NFR-1**: Поддерживаемость
- DRY принцип (Don't Repeat Yourself)
- Единое место хранения данных о группах
- Понятная структура кода

**NFR-2**: Совместимость
- Сохранение mobile-first подхода
- Сохранение offline-first архитектуры
- Сохранение dark theme дизайна

**NFR-3**: Производительность
- Динамическая генерация не должна замедлять загрузку страницы
- Минимальное увеличение размера кода

---

## Пользовательские сценарии

### US-1: Фильтрация по новым группам

**Как** тренер
**Я хочу** видеть кнопки фильтра с реальными названиями классов
**Чтобы** быстро находить учеников нужной группы

**Критерии приемки**:
- [x] Кнопки фильтра отображают М-19, М-117, М-118, А-29, А-218, А-219
- [x] Клик по группе фильтрует список учеников
- [x] Кнопка "Все группы" показывает всех учеников
- [x] Активная кнопка выделена визуально

### US-2: Выбор группы при редактировании

**Как** тренер
**Я хочу** видеть dropdown с реальными названиями классов в форме редактирования
**Чтобы** назначать учеников в правильные группы

**Критерии приемки**:
- [x] Dropdown содержит М-19, М-117, М-118, А-29, А-218, А-219
- [x] Выбранная группа сохраняется корректно
- [x] Дефолтное значение - первая группа из списка (М-19)

### US-3: Легкое изменение групп (для разработчика)

**Как** разработчик
**Я хочу** менять список групп в одном месте
**Чтобы** не искать все упоминания по коду и не пропустить обновления

**Критерии приемки**:
- [x] Все группы в массиве `AVAILABLE_GROUPS`
- [x] Изменение массива автоматически обновляет все UI элементы
- [x] Нет дублирования списка групп в коде

---

## Техническое решение

### Архитектура

**Принцип**: Single Source of Truth

```javascript
// Единственное место определения групп
const AVAILABLE_GROUPS = ['М-19', 'М-117', 'М-118', 'А-29', 'А-218', 'А-219'];

// Динамическая генерация UI из этого источника
renderGroupFilters();  // Кнопки фильтра
renderGroupOptions();  // Dropdown в форме
```

### Компоненты

**1. Массив групп** (`index.html:1288`)
```javascript
const AVAILABLE_GROUPS = ['М-19', 'М-117', 'М-118', 'А-29', 'А-218', 'А-219'];
```

**2. Генератор кнопок фильтра** (`index.html:3360`)
```javascript
function renderGroupFilters() {
    // Создает кнопку "Все группы"
    // Создает кнопки для каждой группы из AVAILABLE_GROUPS
    // Добавляет обработчики событий
}
```

**3. Генератор опций dropdown** (`index.html:3345`)
```javascript
function renderGroupOptions() {
    // Заполняет <select> опциями из AVAILABLE_GROUPS
}
```

**4. Инициализация при загрузке** (`index.html:1850-1851`)
```javascript
document.addEventListener('DOMContentLoaded', () => {
    // ...
    renderGroupFilters();
    renderGroupOptions();
    // ...
});
```

---

## Изменения в коде

### Добавлено

| Файл | Строка | Код |
|------|--------|-----|
| index.html | 1288 | `const AVAILABLE_GROUPS = [...]` |
| index.html | 3345 | `function renderGroupOptions()` |
| index.html | 3360 | `function renderGroupFilters()` |
| index.html | 1850 | Вызов `renderGroupFilters()` |
| index.html | 1851 | Вызов `renderGroupOptions()` |

### Удалено

| Файл | Строки | Что удалено |
|------|--------|-------------|
| index.html | 982-985 | Хардкод HTML кнопок групп |
| index.html | 1037-1040 | Хардкод HTML опций dropdown |
| index.html | 3389-3402 | Старые обработчики событий для кнопок |

### Изменено

| Файл | Строка | Было | Стало |
|------|--------|------|-------|
| index.html | 2625 | `'Начинающие'` | `AVAILABLE_GROUPS[0]` |

**Итого**: +60 строк, -22 строки = net +38

---

## Тестирование

### Unit-тестирование

**Не применимо** - single-file PWA без фреймворка

### Ручное тестирование

**Desktop (Chrome)**:
- [x] Кнопки фильтра отображаются
- [x] Клик по группе фильтрует
- [x] "Все группы" работает
- [x] Dropdown заполнен
- [x] Сохранение группы работает

**Mobile (Safari iOS)** - требует проверки:
- [ ] Touch interactions (44x44px)
- [ ] Визуальный feedback при клике
- [ ] Скроллинг списка групп

**Mobile (Chrome Android)** - требует проверки:
- [ ] Touch interactions
- [ ] Performance
- [ ] Синхронизация с Supabase

### Console logs

```
🏷️ Фильтр групп создан: ['М-19', 'М-117', 'М-118', 'А-29', 'А-218', 'А-219']
📝 Опции групп созданы: ['М-19', 'М-117', 'М-118', 'А-29', 'А-218', 'А-219']
```

---

## Развертывание

**Git коммит**: `d409f90`
```
Feat: Динамическая система групп - замена на реальные классы
```

**GitHub**: https://github.com/usabdnik/WU_Coach2_app

**Деплой**: Автоматический (GitHub Pages)

**Важно для пользователей**:
После обновления очистить кэш браузера или удалить cookies, чтобы Service Worker загрузил новую версию.

---

## Метрики

**LOC (Lines of Code)**:
- Добавлено: 60 строк
- Удалено: 22 строки
- Net: +38 строк

**Сложность**:
- Cyclomatic complexity: Низкая
- Maintainability index: Высокий (DRY принцип)

**Производительность**:
- Время загрузки: Без изменений
- Размер файла: +0.5 KB (незначительно)

---

## Риски и ограничения

### Риски

**R1: Service Worker кэширование**
- **Описание**: Пользователи могут не увидеть новые группы из-за кэша
- **Вероятность**: Высокая
- **Воздействие**: Среднее
- **Митигация**: Инструкция по очистке кэша в деплой уведомлении

**R2: Несовместимость данных в Supabase**
- **Описание**: У существующих учеников могут быть старые названия групп
- **Вероятность**: Высокая
- **Воздействие**: Низкое
- **Митигация**: Дефолтное значение `AVAILABLE_GROUPS[0]` при пустой группе

### Ограничения

**L1**: Нет автоматической миграции данных
- Существующие записи в Supabase с группами "Начинающие" и т.д. не мигрированы
- Требуется ручное обновление через форму редактирования

**L2**: Нет валидации на уровне БД
- Supabase не проверяет допустимость значений группы
- Возможны несовместимые значения при прямом редактировании БД

---

## Будущие улучшения

### V2: Управление группами через UI

**Идея**: Добавить админ-панель для управления группами
- CRUD операции для групп
- Сохранение в Supabase таблице `groups`
- Автоматическая синхронизация с фронтендом

**Преимущества**:
- Не нужно менять код для изменения групп
- История изменений групп
- Права доступа для разных ролей

### V3: Миграция существующих данных

**Идея**: SQL скрипт для обновления старых значений групп

```sql
UPDATE athletes
SET group_name =
  CASE group_name
    WHEN 'Начинающие' THEN 'М-19'
    WHEN 'Средняя' THEN 'М-117'
    -- ...
  END
WHERE group_name IN ('Начинающие', 'Средняя', 'Продвинутая', 'Элитная');
```

### V4: Валидация на уровне БД

**Идея**: CHECK constraint в Supabase

```sql
ALTER TABLE athletes
ADD CONSTRAINT valid_group_name
CHECK (group_name = ANY(ARRAY['М-19', 'М-117', 'М-118', 'А-29', 'А-218', 'А-219']));
```

---

## Связанные документы

- [Feature 005: Schedule & Rank](../005-schedule-rank-subscription/spec.md)
- [CLAUDE.md](../../CLAUDE.md) - Project instructions
- [Constitution](../../.specify/memory/constitution.md) - Architecture principles

---

## Changelog

**2025-11-15**: Feature завершена и задеплоена (d409f90)
