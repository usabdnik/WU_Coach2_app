# Варианты автономной работы Claude Code

**Цель**: Минимизировать ручные действия пользователя при выполнении многозадачных процессов

**Проблема**: Claude не может сам очищать чат и инициировать новые сообщения

---

## Вариант A: Pseudo-автономность (Рекомендуется!)

### Как работает:

1. **Работа в ОДНОЙ длинной сессии** (без частых /clear)
2. После каждой задачи Claude пишет: "✅ Task N done. Жду 'next'"
3. Пользователь просто пишет: **"next"**
4. Claude продолжает сразу (никакой /clear не нужен!)
5. Чистим чат только когда токены кончаются (80-90K+ использовано)

### Преимущества:
- ✅ Минимальное взаимодействие (только "next")
- ✅ Непрерывный контекст между задачами
- ✅ Не нужны внешние скрипты
- ✅ Не ломается при обновлениях Claude Code

### Недостатки:
- ⚠️ Расход токенов (но чистим редко)
- ⚠️ Всё равно нужно писать "next"

### Пример работы:

```
User: "Сделай миграцию на Supabase"

Claude:
📋 План:
Task 1: Установить PostgreSQL client
Task 2: Создать схему БД
Task 3: Мигрировать данные из Google Sheets
Task 4: Интегрировать в PWA
Task 5: Протестировать

Начинаю Task 1...
[работа]
✅ Task 1 done. PostgreSQL client установлен.
Жду 'next' для Task 2.

User: "next"

Claude:
Начинаю Task 2...
[работа]
✅ Task 2 done. Схема создана.
Жду 'next' для Task 3.

... и так далее
```

---

## Вариант B: Watchdog скрипт (UI Automation)

### Как работает:

1. **Внешний скрипт** мониторит файл состояния (например, `.claude/task_status.json`)
2. Claude обновляет этот файл: `{"status": "waiting_for_next", "task": 2}`
3. Скрипт видит статус → симулирует нажатие в UI:
   - Находит окно Claude Code через AppleScript
   - Симулирует клик на input
   - Вводит текст "next"
   - Нажимает Enter
4. Claude продолжает автоматически

### Технические требования:

```bash
# macOS AppleScript для симуляции ввода
osascript <<EOF
tell application "System Events"
    tell process "Electron"
        keystroke "next"
        keystroke return
    end tell
end tell
EOF
```

**Нужны:**
- ✅ macOS Accessibility permissions
- ✅ Найти правильный process name (Electron/Claude)
- ✅ Найти правильное UI-поле для ввода

### Преимущества:
- ✅ Полная автономность (запустил и забыл)
- ✅ Можно работать параллельно с другими задачами

### Недостатки:
- ❌ Хрупкий (ломается при обновлениях UI)
- ❌ Требует Accessibility права
- ❌ Сложная отладка
- ❌ Может конфликтовать с реальными действиями пользователя
- ❌ Не кроссплатформенный (только macOS)

### Структура:

```
project/
├── .claude/
│   ├── task_status.json       # Claude обновляет
│   └── watchdog.sh            # Внешний скрипт
│
└── run_autonomous.sh          # Запуск всего процесса
```

---

## Вариант C: Гибрид - максимум автономности БЕЗ UI automation (BEST!)

### Как работает:

1. Пользователь ОДИН РАЗ пишет: **"выполни все задачи"**
2. Claude создаёт master plan в Serena memory:
   ```
   write_memory("master_plan", {
       task_1: "Install PostgreSQL",
       task_2: "Create schema",
       task_3: "Migrate data",
       ...
   })
   ```
3. **Claude выполняет ВСЕ задачи подряд в одной сессии:**
   - Task 1 → выполняет → пишет статус
   - Task 2 → выполняет → пишет статус
   - Task 3 → выполняет → пишет статус
   - ...
   - Task N → выполняет → "✅ Все задачи завершены!"
4. Периодические чекпоинты:
   ```
   После каждой задачи:
   write_memory("checkpoint_task_2", результаты)
   ```
5. Если прервано или токены кончились:
   - Пользователь: "/clear" → "продолжай"
   - Claude: `read_memory("master_plan")` → продолжает с того места

### Преимущества:
- ✅ Минимальное взаимодействие (1 команда на весь процесс!)
- ✅ Автоматические чекпоинты
- ✅ Можно прервать и продолжить
- ✅ Работает в стандартном Claude Code
- ✅ Не требует внешних скриптов
- ✅ Кроссплатформенный

### Недостатки:
- ⚠️ Если процесс очень длинный → всё равно нужен /clear + "продолжай"
- ⚠️ Нужна дисциплина у пользователя (не прерывать процесс)

### Пример работы:

```
User: "Выполни полную миграцию на Supabase (все задачи автоматом)"

Claude:
📋 Создаю master plan...
✅ План сохранён в memory: 8 задач

🚀 Начинаю автономное выполнение...

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Task 1/8: Установка PostgreSQL client
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[работа 30 секунд]
✅ Завершено. Чекпоинт сохранён.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Task 2/8: Создание схемы БД
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[работа 20 секунд]
✅ Завершено. Чекпоинт сохранён.

... (продолжает все 8 задач подряд)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Task 8/8: Финальное тестирование
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[работа 40 секунд]
✅ Завершено. Чекпоинт сохранён.

🎉 ВСЕ 8 ЗАДАЧ ЗАВЕРШЕНЫ!

📊 Итого:
- Токены использовано: 45,000 / 200,000
- Время: 12 минут
- Все чекпоинты сохранены в memory

⏭️  Можете очистить чат для экономии токенов.
```

---

## Сравнительная таблица

| Критерий | Вариант A | Вариант B | Вариант C |
|----------|-----------|-----------|-----------|
| Автономность | ⭐⭐⭐ (нужен "next") | ⭐⭐⭐⭐⭐ (полная) | ⭐⭐⭐⭐ (почти полная) |
| Простота настройки | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐⭐ |
| Надёжность | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐⭐ |
| Расход токенов | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| Требует внешних скриптов | Нет | Да | Нет |
| Кроссплатформенность | Да | Нет (только macOS) | Да |
| Риск поломки | Низкий | Высокий | Низкий |

---

## Рекомендация

**Для продакшена: Вариант C**

**Почему:**
1. ✅ Максимальная автономность БЕЗ хаков
2. ✅ Работает в стандартном Claude Code
3. ✅ Надёжный (не ломается при обновлениях)
4. ✅ Чекпоинты позволяют прерывать/продолжать
5. ✅ Один раз запустили → все задачи выполнились

**Когда использовать Вариант A:**
- Быстрые задачи (2-5 шагов)
- Нужен контроль после каждого шага

**Когда НЕ использовать Вариант B:**
- Никогда в продакшене (слишком хрупкий)
- Только для экспериментов

---

## Реализация Варианта C

### Для активации:

Пользователь пишет:
```
Выполни [описание большой задачи] автономно
```

или

```
Режим: автономный. Задача: [описание]
```

### Claude автоматически:

1. Создаёт master plan
2. Разбивает на задачи
3. Выполняет все подряд
4. Сохраняет чекпоинты
5. Отчитывается по завершении

### При прерывании (токены кончились):

```
User: /clear → "продолжай"

Claude:
📖 Читаю последний чекпоинт...
✅ Найден: Task 5/8 завершена
🚀 Продолжаю с Task 6/8...
```

---

**Последнее обновление**: 2025-11-10
**Рекомендация**: Вариант C для всех больших задач
**Статус**: Готов к использованию
